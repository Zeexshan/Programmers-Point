I want to deploy on Netlify for FREE, so I can't use Express backend. However, I understand the Google Sheets direct calls are failing. Let's do this:
Phase 1: Fix What We Can in Replit
1. Fix Read-Only Features (Course Explorer, Placements):
These features only READ from Google Sheets and should work with public API key.
Update client/src/utils/googleSheets.ts to handle errors better:
typescriptconst SHEET_ID = import.meta.env.VITE_GOOGLE_SHEET_ID;
const API_KEY = import.meta.env.VITE_GOOGLE_API_KEY;

export async function readSheet(sheetName: string, range = 'A2:Z') {
  const url = `https://sheets.googleapis.com/v4/spreadsheets/${SHEET_ID}/values/${sheetName}!${range}?key=${API_KEY}`;
  
  try {
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json'
      }
    });

    if (!response.ok) {
      const errorData = await response.json();
      console.error('Google Sheets API Error:', errorData);
      throw new Error(errorData.error?.message || 'Failed to fetch data');
    }

    const data = await response.json();
    return data.values || [];
  } catch (error) {
    console.error(`Error reading ${sheetName}:`, error);
    // Return empty array instead of throwing, so UI doesn't break
    return [];
  }
}

export async function fetchAllData() {
  console.log('Fetching data from Google Sheets...');
  console.log('Sheet ID:', SHEET_ID ? 'Set' : 'Missing');
  console.log('API Key:', API_KEY ? 'Set' : 'Missing');

  try {
    const [technologies, combinations, companies, placements] = await Promise.all([
      readSheet('Technologies', 'A2:J'),
      readSheet('Combinations', 'A2:H'),
      readSheet('Companies', 'A2:D'),
      readSheet('Placements', 'A2:I')
    ]);

    console.log('Data fetched:', {
      technologies: technologies.length,
      combinations: combinations.length,
      companies: companies.length,
      placements: placements.length
    });

    return {
      technologies: technologies.map(row => ({
        name: row[0],
        mainCategory: row[1],
        subCategory: row[2],
        displayOrder: parseInt(row[3] || '0'),
        vacancies: parseInt(row[4] || '0'),
        fresherPackage: row[5] || '',
        experiencedPackage: row[6] || '',
        topCompanies: row[7] || '',
        popularityScore: parseInt(row[8] || '5'),
        description: row[9] || ''
      })),
      // ... rest of mapping
    };
  } catch (error) {
    console.error('Error in fetchAllData:', error);
    throw error;
  }
}
2. Disable Inquiry Form for Now:
In client/src/pages/InquiryForm.tsx, add a message:
typescript// At the top of the component
const [formDisabled, setFormDisabled] = useState(true);

// In the JSX, before the form:
{formDisabled && (
  <div className="bg-yellow-100 border-l-4 border-yellow-500 p-4 mb-6">
    <p className="text-yellow-700">
      <strong>Note:</strong> Inquiry form will be enabled after deployment to Netlify. 
      For now, you can explore the Course Explorer and Placements sections.
    </p>
  </div>
)}
3. Fix Navbar React Error:
The nested <a> tags issue - please check client/src/components/Navbar.tsx and ensure you're using React Router's Link component correctly:
typescriptimport { Link } from 'react-router-dom';

// Use Link like this (no nested <a>):
<Link to="/" className="...">Home</Link>

// NOT like this:
<Link to="/"><a>Home</a></Link>

Phase 2: Prepare for Netlify Deployment
1. Create Netlify Function:
Even though we can't test it in Replit, create netlify/functions/submit-inquiry.js:
javascriptconst { google } = require('googleapis');

exports.handler = async (event) => {
  const headers = {
    'Access-Control-Allow-Origin': '*',
    'Access-Control-Allow-Headers': 'Content-Type',
    'Access-Control-Allow-Methods': 'POST, OPTIONS'
  };

  if (event.httpMethod === 'OPTIONS') {
    return { statusCode: 200, headers, body: '' };
  }

  if (event.httpMethod !== 'POST') {
    return { statusCode: 405, headers, body: JSON.stringify({ error: 'Method Not Allowed' }) };
  }

  try {
    const formData = JSON.parse(event.body);

    const auth = new google.auth.GoogleAuth({
      credentials: {
        client_email: process.env.SERVICE_ACCOUNT_EMAIL,
        private_key: process.env.SERVICE_ACCOUNT_PRIVATE_KEY.replace(/\\n/g, '\n')
      },
      scopes: ['https://www.googleapis.com/auth/spreadsheets']
    });

    const sheets = google.sheets({ version: 'v4', auth });

    await sheets.spreadsheets.values.append({
      spreadsheetId: process.env.GOOGLE_SHEET_ID,
      range: 'Inquiries!A:J',
      valueInputOption: 'RAW',
      resource: {
        values: [[
          new Date().toISOString(),
          formData.name,
          formData.fatherName || '',
          formData.phone,
          formData.email,
          formData.dob || '',
          formData.courseInterest,
          formData.college || '',
          formData.branch || '',
          'Pending'
        ]]
      }
    });

    return {
      statusCode: 200,
      headers,
      body: JSON.stringify({ success: true })
    };
  } catch (error) {
    console.error('Error:', error);
    return {
      statusCode: 500,
      headers,
      body: JSON.stringify({ error: 'Failed to submit inquiry' })
    };
  }
};
2. Update InquiryForm to use Netlify Function in production:
typescriptasync function handleSubmit(formData) {
  // Check if we're on Netlify (production)
  const isProduction = window.location.hostname.includes('netlify.app');
  
  if (!isProduction) {
    alert('Inquiry form only works in production (Netlify). Please explore other features!');
    return;
  }

  try {
    const response = await fetch('/.netlify/functions/submit-inquiry', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(formData)
    });

    if (!response.ok) throw new Error('Submission failed');
    
    alert('Inquiry submitted successfully!');
    // Reset form
  } catch (error) {
    alert('Failed to submit. Please try again.');
  }
}

Phase 3: Testing Plan
In Replit (Now):

Test Course Explorer (drag-and-drop technologies)
Test Placements showcase (company cards, student details)
View inquiry form (but can't submit)
Test admin login (view data, but can't save)

On Netlify (After Deploy):

Test inquiry form submission
Verify data saves to Google Sheet
Test admin panel fully


Summary:
What works in Replit:

✅ UI/UX testing
✅ Course explorer (if Google Sheets reads work)
✅ Placements showcase (if Google Sheets reads work)
❌ Inquiry form (needs Netlify Function)

What works on Netlify:

✅ Everything (including inquiry form)

This approach:

✅ FREE deployment
✅ Secure
✅ Can test most features in Replit
✅ Production-ready

Please implement Phase 1 fixes so I can at least test the UI and read-only features in Replit!"