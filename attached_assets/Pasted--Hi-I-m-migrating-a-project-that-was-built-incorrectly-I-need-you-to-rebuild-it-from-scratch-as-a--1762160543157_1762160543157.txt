"Hi! I'm migrating a project that was built incorrectly. I need you to rebuild it from scratch as a FRONTEND-ONLY React app with Google Sheets as the database. No backend server needed.**

---

## 🎯 **PROJECT OVERVIEW:**

**App Name:** Programmers Point - Institute Inquiry & Course Explorer System

**Purpose:**
- Digital inquiry form for students (saves to Google Sheets)
- Interactive course explorer with drag-and-drop (like Infinite Craft game)
- Student placement showcase with company cards
- Admin panel to manage data

**Target Device:** Android tablet (touch-optimized)

**Deployment:** Netlify static hosting (100% FREE)

---

## ⚠️ **CRITICAL REQUIREMENT: NO BACKEND SERVER!**

### **❌ DO NOT BUILD:**
- Express.js server
- Node.js backend
- API routes in server folder
- Any backend framework

### **✅ MUST BUILD:**
- Pure React frontend (Vite)
- Direct Google Sheets API integration from frontend
- Static site that deploys to Netlify for FREE
- All data stored in Google Sheets (5 sheets)

---

## 🏗️ **ARCHITECTURE:**

```
┌─────────────────────────────────────────────┐
│  BROWSER (Tablet/Desktop)                   │
│         ↓                                   │
│  REACT APP (Netlify - Static Hosting)      │
│    - Vite + React + Tailwind               │
│    - Google Sheets API (direct calls)      │
│    - localStorage (caching)                │
│         ↓                                   │
│  GOOGLE SHEETS (Database)                   │
│    Sheet 1: Technologies (44 rows)         │
│    Sheet 2: Combinations (22 rows)         │
│    Sheet 3: Companies (12 rows)            │
│    Sheet 4: Placements (6 rows)            │
│    Sheet 5: Inquiries (form submissions)   │
└─────────────────────────────────────────────┘
```

**NO Express.js, NO server folder, NO backend!**

---

## 📦 **TECH STACK:**

### **Frontend:**
```json
{
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0",
    "@dnd-kit/core": "^6.0.8",
    "@dnd-kit/sortable": "^7.0.2",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.0.0",
    "tailwindcss": "^3.3.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0"
  }
}
```

**NO backend dependencies! NO Express, NO cors, NO body-parser!**

### **Build Tool:**
- Vite (fast, modern, perfect for static sites)

### **Styling:**
- Tailwind CSS (utility-first)

### **Deployment:**
- Netlify (static hosting - FREE forever)

---

## 📊 **GOOGLE SHEETS STRUCTURE:**

I will provide a Google Spreadsheet with 5 sheets:

### **Sheet 1: "Technologies"**
Columns: Technology, Main Category, Sub Category, Display Order, Vacancies, Fresher Package, Experienced Package, Top Companies, Popularity Score, Description

Contains 44 technologies I teach (HTML5, CSS3, JavaScript, React, Python, Java, Node.js, etc.)

### **Sheet 2: "Combinations"**
Columns: Technologies (comma-separated), Job Role, Category, Vacancies, Fresher Package, Experienced Package, Top Companies, Popularity Score

Contains 22 pre-defined technology combinations (e.g., "React.js, Node.js, MongoDB" = MERN Stack Developer)

### **Sheet 3: "Companies"**
Columns: Company Name, Logo URL, Total Placements, Avg Package

Contains 12 companies where students got placed (TCS, Infosys, Wipro, etc.)

### **Sheet 4: "Placements"**
Columns: Student Name, Company, Package, Phone, Photo URL, Profile, Course, Review, Joining Date

Contains 6 sample student placements

### **Sheet 5: "Inquiries"**
Columns: Timestamp, Name, Father Name, Phone, Email, DOB, Course Interest, College, Branch, Status

This is where inquiry form submissions are saved (append-only)

---

## 🔧 **GOOGLE SHEETS API INTEGRATION:**

### **Read Operations (Public Data):**
Use Google Sheets API v4 with API Key:

```javascript
// src/utils/googleSheets.js

const SHEET_ID = import.meta.env.VITE_GOOGLE_SHEET_ID;
const API_KEY = import.meta.env.VITE_GOOGLE_API_KEY;

export async function readSheet(sheetName, range = 'A2:Z') {
  const url = `https://sheets.googleapis.com/v4/spreadsheets/${SHEET_ID}/values/${sheetName}!${range}?key=${API_KEY}`;
  
  try {
    const response = await fetch(url);
    if (!response.ok) throw new Error('Failed to fetch');
    const data = await response.json();
    return data.values || [];
  } catch (error) {
    console.error(`Error reading ${sheetName}:`, error);
    throw error;
  }
}

// Fetch all sheets data
export async function fetchAllData() {
  const [technologies, combinations, companies, placements] = await Promise.all([
    readSheet('Technologies', 'A2:J'),
    readSheet('Combinations', 'A2:H'),
    readSheet('Companies', 'A2:D'),
    readSheet('Placements', 'A2:I')
  ]);

  return {
    technologies: technologies.map(row => ({
      name: row[0],
      mainCategory: row[1],
      subCategory: row[2],
      displayOrder: parseInt(row[3]),
      vacancies: parseInt(row[4]),
      fresherPackage: row[5],
      experiencedPackage: row[6],
      topCompanies: row[7],
      popularityScore: parseInt(row[8]),
      description: row[9]
    })),
    combinations: combinations.map(row => ({
      technologies: row[0].split(', '),
      jobRole: row[1],
      category: row[2],
      vacancies: parseInt(row[3]),
      fresherPackage: row[4],
      experiencedPackage: row[5],
      topCompanies: row[6],
      popularityScore: parseInt(row[7])
    })),
    companies: companies.map(row => ({
      name: row[0],
      logoUrl: row[1],
      totalPlacements: parseInt(row[2]),
      avgPackage: row[3]
    })),
    placements: placements.map(row => ({
      studentName: row[0],
      company: row[1],
      package: row[2],
      phone: row[3],
      photoUrl: row[4],
      profile: row[5],
      course: row[6],
      review: row[7],
      joiningDate: row[8]
    }))
  };
}
```

### **Write Operations (Inquiry Form):**
Use Service Account for authentication:

```javascript
export async function appendToInquiries(formData) {
  const SERVICE_ACCOUNT_EMAIL = import.meta.env.VITE_SERVICE_ACCOUNT_EMAIL;
  const PRIVATE_KEY = import.meta.env.VITE_SERVICE_ACCOUNT_PRIVATE_KEY;
  
  // Get OAuth2 token
  const token = await getAccessToken(SERVICE_ACCOUNT_EMAIL, PRIVATE_KEY);
  
  const url = `https://sheets.googleapis.com/v4/spreadsheets/${SHEET_ID}/values/Inquiries!A:J:append?valueInputOption=RAW`;
  
  const row = [
    new Date().toISOString(),
    formData.name,
    formData.fatherName,
    formData.phone,
    formData.email,
    formData.dob,
    formData.courseInterest,
    formData.college,
    formData.branch,
    'Pending'
  ];

  const response = await fetch(url, {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${token}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({ values: [row] })
  });

  if (!response.ok) throw new Error('Failed to submit inquiry');
  return await response.json();
}

// Generate OAuth2 token from service account
async function getAccessToken(email, privateKey) {
  // Use @googleapis/sheets or implement JWT signing
  // This needs to work in browser (frontend)
  // Use a lightweight JWT library compatible with browser
  
  const jwt = await createJWT(email, privateKey);
  const response = await fetch('https://oauth2.googleapis.com/token', {
    method: 'POST',
    headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
    body: new URLSearchParams({
      grant_type: 'urn:ietf:params:oauth:grant-type:jwt-bearer',
      assertion: jwt
    })
  });
  
  const data = await response.json();
  return data.access_token;
}
```

### **Caching Strategy:**
Cache data in localStorage to reduce API calls:

```javascript
const CACHE_DURATION = 3600000; // 1 hour

export function getCachedData(key) {
  try {
    const cached = localStorage.getItem(key);
    if (!cached) return null;
    
    const { data, timestamp } = JSON.parse(cached);
    
    if (Date.now() - timestamp > CACHE_DURATION) {
      localStorage.removeItem(key);
      return null;
    }
    
    return data;
  } catch {
    return null;
  }
}

export function setCachedData(key, data) {
  try {
    localStorage.setItem(key, JSON.stringify({
      data,
      timestamp: Date.now()
    }));
  } catch (error) {
    console.error('Cache error:', error);
  }
}

export function clearCache() {
  localStorage.removeItem('allData');
}
```

---

## 📁 **PROJECT STRUCTURE:**

```
programmers-point-app/
├── public/
│   └── (static assets)
├── src/
│   ├── components/
│   │   ├── Navbar.jsx
│   │   ├── Footer.jsx
│   │   ├── TechnologyCard.jsx
│   │   ├── CompanyCard.jsx
│   │   ├── StudentCard.jsx
│   │   └── LoadingSpinner.jsx
│   ├── pages/
│   │   ├── Home.jsx
│   │   ├── CourseExplorer.jsx
│   │   ├── Placements.jsx
│   │   ├── InquiryForm.jsx
│   │   └── admin/
│   │       ├── Dashboard.jsx
│   │       ├── Login.jsx
│   │       ├── Technologies.jsx
│   │       ├── Combinations.jsx
│   │       ├── Companies.jsx
│   │       ├── Placements.jsx
│   │       └── Inquiries.jsx
│   ├── utils/
│   │   ├── googleSheets.js (ALL Google Sheets logic here)
│   │   └── helpers.js
│   ├── App.jsx
│   ├── main.jsx
│   └── index.css
├── .env.example
├── .gitignore
├── netlify.toml
├── package.json
├── vite.config.js
├── tailwind.config.js
├── postcss.config.js
└── README.md
```

**NO server/ folder, NO backend files!**

---

## 🎨 **FEATURES TO IMPLEMENT:**

### **1. Home Page**
- Hero section with institute name "Programmers Point"
- Brief description
- "Start Your Journey" button (proper top margin: mt-12)
- Navigation to Course Explorer, Placements, Inquiry Form

### **2. Course Explorer (Main Feature)**

**Layout:**
```
┌────────────────────────────────────────────┐
│  Available Technologies (Grouped)          │
│  ┌──────────────┐ ┌──────────────┐        │
│  │ Frontend     │ │ Backend      │        │
│  │ - HTML5      │ │ - Python     │        │
│  │ - CSS3       │ │ - Java       │        │
│  │ - JavaScript │ │ - Node.js    │        │
│  │ - React.js   │ │ - PHP        │        │
│  └──────────────┘ └──────────────┘        │
│                                            │
│  Selected Technologies:                    │
│  [React.js ✕] [Node.js ✕] [MongoDB ✕]    │
│                                            │
│  ↓ Result ↓                                │
│  ┌────────────────────────────────────────┐│
│  │ 🎯 MERN Stack Developer                ││
│  │ Combining 3 technologies               ││
│  │                                        ││
│  │ 💰 Fresher: 4-8 LPA                   ││
│  │ 💼 Experienced: 8-16 LPA              ││
│  │ 📊 Vacancies: 2,500+                  ││
│  │ 🏢 Companies: Flipkart, Swiggy, Zomato││
│  │                                        ││
│  │ [🚀 Learn This Stack]                 ││
│  └────────────────────────────────────────┘│
└────────────────────────────────────────────┘
```

**Functionality:**
- Display technologies grouped by Main Category (Frontend, Backend, Database, Other)
- Each category further grouped by Sub Category
- Click to select (no actual drag needed - mobile friendly)
- Show selected technologies as removable chips
- Real-time calculation of result

**Matching Logic:**
1. **Exact Match:** Check if selected technologies match any combination in Sheet 2
   - Example: React.js, Node.js, MongoDB → Finds "MERN Stack Developer" with exact data
   
2. **Calculated:** If no match, calculate from individual technologies in Sheet 1
   - Sum vacancies
   - Average packages
   - Merge companies (unique)
   - Generic title like "React.js, Node.js & MongoDB Developer"

3. **Display:** Show result card with all details

### **3. Placements Showcase**

**Layout:**
```
┌────────────────────────────────────────────┐
│  Our Placements                            │
│                                            │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐   │
│  │  [TCS]  │  │[Infosys]│  │ [Wipro] │   │
│  │ 15 placed│  │12 placed│  │ 8 placed│   │
│  │ 4.5 LPA │  │ 5.2 LPA │  │ 4.8 LPA │   │
│  └─────────┘  └─────────┘  └─────────┘   │
└────────────────────────────────────────────┘

When click TCS:
┌────────────────────────────────────────────┐
│  TCS Placements (15)                       │
│                                            │
│  ┌────────────────────────────────┐       │
│  │ [Photo] Sachin Kumar           │       │
│  │ Package: 4 LPA                 │       │
│  │ Profile: Java Developer        │       │
│  │ Course: Core Java + Advanced   │       │
│  │ Phone: ********* [Show]        │       │
│  │ Review: "Great training..."    │       │
│  └────────────────────────────────┘       │
└────────────────────────────────────────────┘
```

**Functionality:**
- Read Companies from Sheet 3
- Show company cards with logo, name, placements, package
- Click company → Filter placements from Sheet 4
- Show student cards with masked phone numbers
- Click "Show Phone" button → reveals number

### **4. Inquiry Form**

**Fields:**
- Name (required)
- Father's Name (required)
- Phone (+91XXXXXXXXXX format, required)
- Email (required, email validation)
- Date of Birth (date picker)
- Course Interest (dropdown from combinations, required)
- College (text)
- Branch (dropdown: CSE, IT, ECE, etc.)

**Submit Action:**
- Validate all fields
- Call `appendToInquiries()` function
- Show loading spinner
- On success: Show success message + clear form
- On error: Show error message
- Data saved to Sheet 5 "Inquiries" with Status = "Pending"

### **5. Admin Panel**

**Login:**
- Route: `/admin`
- Username: `admin` (hardcoded)
- Password: from `VITE_ADMIN_PASSWORD` env variable
- Store login state in sessionStorage (not localStorage)

**Dashboard:**
- Total inquiries count
- Pending inquiries count
- Joined students count
- Total placements count
- Recent inquiries table (last 10)

**Manage Technologies:**
- List all technologies from Sheet 1
- Search/filter by category
- Edit button (opens modal with all fields)
- On save: Update Google Sheet row
- Note: Add/Delete not needed (I'll edit sheet directly)

**Manage Combinations:**
- List all combinations from Sheet 2
- Each shows: Technologies, Job Role, Vacancies, Packages
- Edit button (modal with all fields)
- **CRITICAL:** Sidebar must stay visible on this page!

**Manage Companies:**
- List all companies from Sheet 3
- Edit button for company details

**Manage Placements:**
- List all placements from Sheet 4
- Filter by company
- Edit button for student details

**View Inquiries:**
- List all from Sheet 5
- Filter by Status (All/Pending/Joined)
- Search by name/phone
- "Export Phone Numbers" button → Downloads CSV with phones of Pending inquiries
- "Mark as Joined" button → Updates Status in Sheet 5

**Refresh Data Button:**
- Clears cache
- Refetches all data from Google Sheets
- Shows success message

---

## 🎯 **CRITICAL DESIGN REQUIREMENTS:**

### **Header/Navbar:**
- Logo: "Programmers Point" text or image
- Padding: 20-30px around logo (not touching edges!)
- Height: 64-72px
- Shadow: subtle (shadow-sm)
- Navigation links: Home, Courses, Placements, Inquiries, Admin
- Responsive: hamburger menu on mobile

### **Buttons:**
- Minimum height: 48px (touch-friendly)
- "Start Your Journey" button: Top margin of 48px (mt-12)
- Proper spacing around all buttons

### **Admin Sidebar:**
- Must be visible on ALL admin pages
- **Especially on Tech Combinations page** (previously broken)
- Links: Dashboard, Technologies, Combinations, Companies, Placements, Inquiries, Logout

### **Responsive:**
- Mobile: 375px width
- Tablet: 768px, 1024px
- Desktop: 1280px+
- Touch-friendly on all devices
- No horizontal scrolling

### **Loading States:**
- Show spinner while fetching data
- Show skeleton loaders for cards
- Never show blank screens

### **Error Handling:**
- Friendly error messages
- Retry buttons
- Offline indicators

---

## 🔐 **ENVIRONMENT VARIABLES:**

**.env.example:**
```bash
# Google Sheets Configuration
VITE_GOOGLE_SHEET_ID=
VITE_GOOGLE_API_KEY=
VITE_SERVICE_ACCOUNT_EMAIL=
VITE_SERVICE_ACCOUNT_PRIVATE_KEY=

# Admin Panel
VITE_ADMIN_PASSWORD=admin123
```

**I will provide actual values after you build the app.**

---

## 📦 **BUILD & DEPLOYMENT:**

### **Build Command:**
```bash
npm ci && npm run build
```

### **Output:**
- Creates `dist/` folder with static files
- All JavaScript bundled
- All CSS bundled
- Assets optimized

### **netlify.toml:**
```toml
[build]
  command = "npm ci && npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
```

### **Deployment to Netlify:**
1. Connect GitHub repository
2. Set build command: `npm ci && npm run build`
3. Set publish directory: `dist`
4. Add environment variables
5. Deploy!

**Result:** `https://programmers-point.netlify.app` (FREE forever!)

---

## ✅ **TESTING CHECKLIST:**

Before marking as complete, test these:

### **Data Fetching:**
- [ ] Technologies load from Sheet 1
- [ ] Combinations load from Sheet 2
- [ ] Companies load from Sheet 3
- [ ] Placements load from Sheet 4
- [ ] Data cached in localStorage
- [ ] Cache expires after 1 hour
- [ ] Refresh button clears cache

### **Course Explorer:**
- [ ] Technologies grouped by category
- [ ] Click to select technology
- [ ] Selected shows as chip with X button
- [ ] Click X removes technology
- [ ] Selecting "React.js, Node.js, MongoDB" shows "MERN Stack Developer" (exact match from Sheet 2)
- [ ] Selecting "HTML5, CSS3, Bootstrap" calculates from Sheet 1 (no exact match)
- [ ] Result shows correct vacancies, packages, companies
- [ ] Works on mobile (touch-friendly)

### **Placements:**
- [ ] Company cards load with logos
- [ ] Click company shows students
- [ ] Student cards show all details
- [ ] Phone numbers masked by default
- [ ] Show phone button works

### **Inquiry Form:**
- [ ] All fields validate
- [ ] Phone number accepts +91 format
- [ ] Email validation works
- [ ] Date picker works
- [ ] Course dropdown populated from combinations
- [ ] Submit button disabled during submission
- [ ] Success message shows after submit
- [ ] Data appears in Sheet 5
- [ ] Form clears after success

### **Admin Panel:**
- [ ] Login works with correct password
- [ ] Wrong password shows error
- [ ] Dashboard shows correct counts
- [ ] Sidebar visible on ALL pages including Tech Combinations
- [ ] Edit technology works and updates Sheet 1
- [ ] Edit combination works and updates Sheet 2
- [ ] Edit company works and updates Sheet 3
- [ ] Edit placement works and updates Sheet 4
- [ ] View inquiries shows data from Sheet 5
- [ ] Filter by status works
- [ ] Export phone numbers downloads CSV
- [ ] Refresh data button works

### **UI/UX:**
- [ ] Header has proper padding (logo not touching edges)
- [ ] "Start Your Journey" button has mt-12
- [ ] All buttons minimum 48px height
- [ ] Responsive on mobile/tablet/desktop
- [ ] No horizontal scrolling
- [ ] Loading spinners show
- [ ] Error messages user-friendly

### **Build:**
- [ ] `npm run build` works without errors
- [ ] `dist/` folder generated
- [ ] All assets bundled correctly
- [ ] Environment variables work with VITE_ prefix

---

## 📝 **DELIVERABLES:**

Please provide:

1. **Complete source code** (no server folder!)
2. **.env.example** with all variable names
3. **README.md** with:
   - Project description
   - Setup instructions
   - How to get Google Sheets credentials
   - How to deploy to Netlify
   - Environment variables explanation
4. **netlify.toml** configured
5. **package.json** with correct scripts
6. **Confirmation that:**
   - No backend server exists
   - All API calls are from frontend
   - App builds successfully
   - All tests pass

---

## 🚫 **WHAT NOT TO DO:**

- ❌ DO NOT create Express.js server
- ❌ DO NOT create server/ folder
- ❌ DO NOT use backend frameworks
- ❌ DO NOT add backend dependencies
- ❌ DO NOT create API routes
- ❌ DO NOT use Node.js server
- ❌ DO NOT make this a full-stack app

**This is a FRONTEND-ONLY static React app!**

---

## ⏰ **TIMELINE:**

This is a fresh rebuild. Please:
1. Set up Vite + React project
2. Implement Google Sheets integration (frontend only)
3. Build all pages and features
4. Test everything
5. Confirm builds successfully

**Estimated time?**

**Once complete, I'll deploy to Netlify and use on my tablet. The project will be 100% free forever with no server costs!** 🎉"